{
  "name": "harness",
  "version": "0.1.0",
  "description": "Minimal pi outer harness — guides + sensors + file memory — for learning",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "sleep": "tsx memory/sleep.ts",
    "eval": "tsx sensors/run-eval.ts"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.0"
  }
}
